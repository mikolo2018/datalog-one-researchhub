'use client'
import { FormEvent, useState } from 'react'
export default function Consultation(){ const [msg,setMsg]=useState('')
async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault(); const f=new FormData(e.currentTarget); const body={type:f.get('type'),scheduledAt:f.get('scheduledAt'),notes:f.get('notes')}; const r=await fetch('/api/consultations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}); const j=await r.json(); setMsg(j.error||'Consultation request received. A Datalog consultant will follow up.')}
return <main className="shell narrow"><section className="card"><div className="eyebrow">EXPERT REVIEW</div><h1>Book a Datalog Consultant</h1><p className="muted">Escalate complex methodology, analysis, publication or defence questions to a human expert.</p><form onSubmit={submit} className="stack"><label>Review type<select name="type"><option value="methodology">Methodology review</option><option value="statistics">Statistical analysis review</option><option value="publication">Publication readiness</option><option value="defence">Defence preparation</option></select></label><label>Preferred date/time<input name="scheduledAt" type="datetime-local" /></label><label>What do you need help with?<textarea name="notes" rows={6} required /></label><button className="btn">Request consultation</button></form>{msg&&<p className="notice">{msg}</p>}<a className="secondary-btn" href="https://wa.me/2348166414241?text=Hello%20Datalog%2C%20I%20need%20an%20expert%20research%20consultation." target="_blank">Continue on WhatsApp</a></section></main> }