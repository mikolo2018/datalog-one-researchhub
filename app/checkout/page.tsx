'use client'
import { FormEvent, useState } from 'react'
const plans=[['Student','3500','Full topic packs, detailed diagnostics, templates'],['Researcher','8500','Advanced diagnostics, publication toolkit, priority support'],['Expert Review','20000','Human review of methodology or analysis']]
export default function Checkout(){const [msg,setMsg]=useState(''); async function pay(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/paystack/initialize',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),amountNaira:f.get('amount'),purpose:f.get('plan')})});const j=await r.json();if(j.authorization_url) window.location.href=j.authorization_url; else setMsg(j.error||'Unable to start payment')}
return <main className="shell"><div className="eyebrow">UPGRADE</div><h1>Choose the support you need</h1><div className="grid3">{plans.map(([n,p,d])=><form className="card price" key={n} onSubmit={pay}><h2>{n}</h2><div className="price-value">₦{Number(p).toLocaleString()}</div><p>{d}</p><input type="hidden" name="plan" value={n}/><input type="hidden" name="amount" value={p}/><label>Email<input name="email" type="email" required/></label><button className="btn">Continue to Paystack</button></form>)}</div>{msg&&<p className="notice">{msg}</p>}</main>}